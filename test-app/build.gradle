plugins {
    id 'scala'
    id 'maven-publish'
    id 'com.github.johnrengelman.shadow' version '6.1.0'
    id 'application'
}

group 'com.itsumma'
version '2.1.0'
description = ''

sourceSets {
    main {
        scala {
            srcDirs = ['src']
        }
    }
}

repositories {
    mavenCentral()
    //mavenLocal()

    maven { url "https://download.osgeo.org/webdav/geotools/" }
}

dependencies {
    implementation group: 'org.apache.spark', name: 'spark-streaming-kafka-0-10_2.11', version:'2.4.7'
    implementation group: 'org.apache.spark', name: 'spark-sql-kafka-0-10_2.11', version:'2.4.7'
    implementation group: 'com.vividsolutions', name: 'jts', version:'1.13'
    compileOnly group: 'org.apache.spark', name: 'spark-streaming_2.11', version:'2.4.7'
}

application {
    mainClassName = 'com.itsumma.gpconnector.testapp.ItsGpConnectorTestApp'
}

shadowJar {
    archiveBaseName.set('test-app_2.11')
    archiveClassifier.set('')
    archiveVersion.set('2.1')
    minimize{
        exclude(dependency('org.postgresql:.*:.*'))
    }
}

task copyJarToBin(type: Copy) {
    from shadowJar.outputs.getFiles()[0]
    //from jar.archiveFile.toString()
    into "/home/hadoop"
}
